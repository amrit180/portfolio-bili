import { CodepenCircleFilled } from "@ant-design/icons";
const Icon = ({ style }) => {
  return (
    <div style={style}>
      <CodepenCircleFilled className="iconsicon" />
    </div>
  );
};
export default Icon;
